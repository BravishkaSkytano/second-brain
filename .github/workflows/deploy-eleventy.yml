name: Deploy Eleventy to GitHub Pages

on:
  push:
    branches: ["master"]
    paths-ignore:
      - ".github/**"
      - "README.md"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      ELEVENTY_ENV: production
      NODE_ENV: production

    steps:
      - name: Checkout repo (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      # 🔥 Cache Eleventy Image + Sharp cache
      - name: Cache Eleventy image cache
        uses: actions/cache@v4
        with:
          path: |
            .node_modules/.cache/@11ty/eleventy-img
            node_modules/.cache/sharp
          key: eleventy-img-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            eleventy-img-${{ runner.os }}-

      - name: Install dependencies
        run: npm ci

      - name: Build site
        run: npm run build

      - name: Run Pagefind
        run: npm run pagefind

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
